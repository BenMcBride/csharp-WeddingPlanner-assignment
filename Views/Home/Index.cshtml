@{
  ViewData["Title"] = "Home Page";
}

<div class="container">
  <div class="row">
    <div class="col-md-6">@await Html.PartialAsync("_Register")</div>
    <div class="col-md-6">@await Html.PartialAsync("_Login")</div>
  </div>
</div>

